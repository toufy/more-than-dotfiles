return {
	stylua = require("formatters.stylua"),
	isort = require("formatters.isort"),
	black = require("formatters.black"),
	clang_format = require("formatters.clang_format"),
	prettier = require("formatters.prettier"),
	google_java_format = require("formatters.google_java_format"),
	shfmt = require("formatters.shfmt"),
}
