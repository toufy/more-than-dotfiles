#!/bin/bash
set -x

# workaround to ensure `$USER` inside the containers
arr=("$@")
for i in "${!arr[@]}"; do
    if [[ ${arr[i]} == *"root:root"* ]]; then
        arr[i]=${arr[i]//"root:root"/"${USER}:${USER}"}
    fi
done

flatpak-spawn --host podman "${arr[@]}"
